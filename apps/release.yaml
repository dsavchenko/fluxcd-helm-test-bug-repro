apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: testapp
  namespace: default
spec:
  chart:
    spec:
      chart: ../helm-test-chart
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  interval: 1m
  timeout: 1m
