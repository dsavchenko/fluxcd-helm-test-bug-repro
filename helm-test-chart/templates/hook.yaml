apiVersion: batch/v1
kind: Job
metadata:
  annotations:
    helm.sh/hook: post-install
  name: simple-hook
spec:
  template:
    spec:
      containers:
        - name: dummy
          image: busybox
          command: 
            - sleep
            - "10"
      restartPolicy: Never
