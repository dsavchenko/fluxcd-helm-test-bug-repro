apiVersion: v1
kind: Pod
metadata:
  name: test2
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: dummy
      image: busybox
      command: ['sleep']
      args: ['120']
  restartPolicy: Never
